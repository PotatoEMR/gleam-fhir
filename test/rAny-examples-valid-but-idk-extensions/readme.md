these use primitive extensionse for required fields
eg questionnaireresponse.questionnaire
kind of awkward if we a) refuse to make primitives more complicated b) refuse to make 1..1 an Option
so probably going to make users do custom codegen then
