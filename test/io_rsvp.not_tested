import fhir/r4/resources
import fhir/r4/complex_types as ct
import fhir/r4/primitive_types as pt
import fhir/r4/client_rsvp

import gleam/option.{Some}

pub fn main() {
  patient_test()
}

// more complicated than httpc to test
// for now just using httpc to check against server
// may test implementation of wrapper over rsvp later
fn patient_test() {
  let fc = client_rsvp.fhirclient_new("https://hapi.fhir.org/baseR4")

  let patient = resources.Patient(..resources.patient_new(), active: Some(True))
}
